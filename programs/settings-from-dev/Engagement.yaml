version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Engagement.yaml (entity settings only) from dev
  audit.
entities:
  Engagement:
    action: create
    type: BasePlus
    labelSingular: Engagement
    labelPlural: Engagements
    stream: true
    settings:
      orderBy: createdAt
      order: desc
      textFilterFields:
      - name
      fullTextSearch: true
      multipleAssignedUsers: true

version: '1.0'
content_version: 1.0.0
description: Settings-only slice of SponsorProfile.yaml (entity settings only) from
  dev audit.
entities:
  SponsorProfile:
    action: create
    type: BasePlus
    labelSingular: Sponsor
    labelPlural: Sponsors
    stream: true
    settings:
      orderBy: createdAt
      order: desc
      textFilterFields:
      - name
      fullTextSearch: false

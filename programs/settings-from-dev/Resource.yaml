version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Resource.yaml (entity settings only) from dev
  audit.
entities:
  Resource:
    action: create
    type: BasePlus
    labelSingular: Resource
    labelPlural: Resources
    stream: true
    settings:
      orderBy: createdAt
      order: desc

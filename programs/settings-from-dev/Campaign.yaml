version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Campaign.yaml (entity settings only) from dev
  audit.
entities:
  Campaign:
    settings:
      orderBy: createdAt
      order: desc
      iconClass: fas fa-chart-line

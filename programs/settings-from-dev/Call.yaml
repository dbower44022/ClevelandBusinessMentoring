version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Call.yaml (entity settings only) from dev audit.
entities:
  Call:
    settings:
      orderBy: dateStart
      order: desc
      iconClass: fas fa-phone

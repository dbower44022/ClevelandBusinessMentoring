version: '1.0'
content_version: 1.0.0
description: Settings-only slice of EventRegistration.yaml (entity settings only)
  from dev audit.
entities:
  EventRegistration:
    action: create
    type: BasePlus
    labelSingular: Event Registration
    labelPlural: Event Registrations
    stream: false
    settings:
      orderBy: createdAt
      order: desc

version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Task.yaml (entity settings only) from dev audit.
entities:
  Task:
    settings:
      orderBy: createdAt
      order: desc
      optimisticConcurrencyControl: true
      iconClass: fas fa-tasks
      kanbanViewMode: true

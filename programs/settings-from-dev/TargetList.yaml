version: '1.0'
content_version: 1.0.0
description: Settings-only slice of TargetList.yaml (entity settings only) from dev
  audit.
entities:
  TargetList:
    settings:
      orderBy: createdAt
      order: desc
      iconClass: fas fa-crosshairs

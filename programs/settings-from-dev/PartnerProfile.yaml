version: '1.0'
content_version: 1.0.0
description: Settings-only slice of PartnerProfile.yaml (entity settings only) from
  dev audit.
entities:
  PartnerProfile:
    action: create
    type: BasePlus
    labelSingular: Partner
    labelPlural: Partners
    stream: true
    settings:
      orderBy: createdAt
      order: desc
      textFilterFields:
      - name
      fullTextSearch: false

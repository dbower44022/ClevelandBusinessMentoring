version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Event.yaml (entity settings only) from dev audit.
entities:
  Event:
    action: create
    type: Event
    labelSingular: Event
    labelPlural: Events
    stream: true
    settings:
      orderBy: dateStart
      order: desc
      textFilterFields:
      - name
      fullTextSearch: true

version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Meeting.yaml (entity settings only) from dev audit.
entities:
  Meeting:
    settings:
      orderBy: dateStart
      order: desc
      iconClass: fas fa-calendar-check

version: '1.0'
content_version: 1.0.0
description: Settings-only slice of Lead.yaml (entity settings only) from dev audit.
entities:
  Lead:
    settings:
      orderBy: createdAt
      order: desc
      textFilterFields:
      - name
      - accountName
      - emailAddress
      iconClass: fas fa-address-card
      color: '#d6a2c9'
